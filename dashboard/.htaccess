# Block direct access to sensitive files
<Files "config.php">
    Require all denied
</Files>

<FilesMatch "\.json$">
    Require all denied
</FilesMatch>

# Block direct access to src/ and data/ subdirectories
RewriteEngine On
RewriteRule ^(src|data)/ - [F,L]

Options -Indexes
